#!/data/data/com.termux/files/usr/bin/bash
# Folder wallpaper
WALLPAPERS="$HOME/storage/pictures/wallpaper"
FILES=("$WALLPAPERS"/*)
COUNT=${#FILES[@]}
INDEX=$((RANDOM % COUNT))
SELECTED="${FILES[$INDEX]}"

# Terapkan wallpaper
termux-wallpaper -f "$SELECTED"
